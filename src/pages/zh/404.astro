---
import NotFound from '../../components/NotFound.astro';

// Localized 404: Cloudflare Pages serves the nearest 404.html walking up the
// directory tree, so misses under this locale prefix get this page. Copy and
// chrome localize from the URL via NotFound.astro.
---

<NotFound />
