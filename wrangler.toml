name = "wavekat-com"
compatibility_date = "2024-04-05"
pages_build_output_dir = "./dist"

[env.production]
# Build command for Cloudflare Pages dashboard: make build
# Requires Node 22 — set NODE_VERSION = 22 in Pages environment variables
